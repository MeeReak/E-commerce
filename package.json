{
  "name": "e-commerce",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"yarn dev --prefix frontend\" \"yarn dev --prefix backend\"",
    "build": "yarn workspace frontend build",
    "start:frontend": "yarn workspace frontend dev",
    "start:backend": "yarn workspace backend start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "next": "^15.1.3",
    "picocolors": "^1.1.1"
  }
}
